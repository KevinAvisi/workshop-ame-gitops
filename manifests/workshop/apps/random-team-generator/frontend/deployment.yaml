---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: random-team-generator
spec:
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: app
        image: registry.gitlab.com/koen.konst/random-teams-generator/frontend:1-2
---
